node_modules
dist
# Artefato do alvo web (Fase 10). O padrao `dist` acima casa so com o nome
# exato; sem esta linha o primeiro `npm run build:web` despeja ~200 arquivos
# no `git status` — e `git add` amplo ja quebrou o main uma vez (HANDOFF no 5).
dist-web
out
.DS_Store
.eslintcache
*.log*

# Segredos e ambiente — nada disso vai pro repo.
# Convex escreve .env.local sozinho; a API key do WorkOS mora nele.
.env
.env.*
!.env.example
!.env.local.example

# Credenciais de infra (token de DNS da Hostinger, chaves do LiveKit)
infra/**/.env
infra/**/*.pem
infra/**/*.key
