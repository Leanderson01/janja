---
phase: 08.5-repaginacao-da-ui
plan: 08
type: execute
wave: 4
depends_on: ["08.5-02", "08.5-03"]
files_modified:
  - src/renderer/src/components/shell/ChannelSidebar.tsx
  - src/renderer/src/lib/sidebar-preferences.ts
  - src/renderer/src/lib/sidebar-preferences.test.ts
autonomous: true

must_haves:
  truths:
    - "As seções TEXTO e VOZ recolhem e expandem por clique e por teclado, com indicador visual de estado"
    - "O estado recolhido sobrevive ao reinício do app, por máquina, sem gravar nada no Convex"
    - "O canal selecionado é identificável pelo tom de destaque da fase, não só por um cinza a mais"
    - "Nenhuma cor direta resta na sidebar de canais: anel de fala e ícone de compartilhamento usam tokens semânticos"
  artifacts:
    - path: "src/renderer/src/lib/sidebar-preferences.ts"
      provides: "Persistência de quais seções estão recolhidas, em localStorage, defensiva contra dado corrompido"
      contains: "janja:sidebar-preferences"
    - path: "src/renderer/src/lib/sidebar-preferences.test.ts"
      provides: "Contrato de persistência e de tolerância a storage ausente/corrompido"
      min_lines: 25
    - path: "src/renderer/src/components/shell/ChannelSidebar.tsx"
      provides: "Seções TEXTO/VOZ como Collapsible, com aria-expanded do Radix e chevron"
      contains: "Collapsible"
  key_links:
    - from: "src/renderer/src/components/shell/ChannelSidebar.tsx"
      to: "src/renderer/src/lib/sidebar-preferences.ts"
      via: "estado inicial lido de loadSidebarPreferences(); toda alternância chama saveSidebarPreferences()"
      pattern: "SidebarPreferences"
---

<objective>
Transformar os dois cabeçalhos de seção fixos (TEXTO / VOZ) em seções
recolhíveis de verdade, com estado que sobrevive ao reinício, e alinhar a
sidebar às convenções de composição da fase.

Purpose: é o item "Navegação e menus" do CONTEXT.md que não depende de menu
nenhum — dá para entregar em paralelo com os outros cinco planos desta onda.
`Collapsible` do Radix (já instalado pelo Plano 08.5-02) resolve
`aria-expanded`, animação de altura e estado aberto/fechado; construir isso à
mão com `useState` + altura animada é a primeira linha da tabela "Don't
Hand-Roll" do RESEARCH.
Output: seções recolhíveis com chevron, persistência por máquina, e a sidebar
sem nenhuma cor direta.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.5-repaginacao-da-ui/08.5-RESEARCH.md
@.planning/phases/08.5-repaginacao-da-ui/08.5-01-SUMMARY.md
@.planning/phases/08.5-repaginacao-da-ui/08.5-02-SUMMARY.md
@src/renderer/src/components/shell/ChannelSidebar.tsx
@src/renderer/src/lib/screenshare-preferences.ts
@src/renderer/src/lib/screenshare-preferences.test.ts

# ORDEM MUDOU (decisão do usuário em 2026-08-19): este plano saiu da onda 3
# para a onda 4 porque o Plano 08.5-03 (palco da call) edita
# `ChannelSidebar.tsx` antes — ele muda a SEMÂNTICA do clique no canal de voz
# (clicar no canal onde já se está passa a voltar ao palco, não a
# desconectar). Reler `08.5-03-SUMMARY.md` e o arquivo real antes de editar;
# o `Collapsible` envolve as linhas de canal já com o comportamento novo.
#
# `screenshare-preferences.ts` + seu teste estão no contexto como MOLDE: são o
# padrão do projeto para preferência de máquina em localStorage, incluindo o
# detalhe de que o ambiente de teste (`edge-runtime`) NÃO tem `localStorage`,
# o que torna o caminho "storage indisponível" o estado nativo do teste.
#
# Propriedade de arquivo: este plano é o único dono de ChannelSidebar.tsx
# nesta onda. Os MENUS de servidor e de canal neste mesmo arquivo são o Plano
# 08.5-12, que roda na onda 4 exatamente por isso — dois agentes reescrevendo
# ChannelSidebar.tsx em paralelo se atropelam (HANDOFF.md, lição nº 6).
# NÃO adicionar DropdownMenu aqui; deixar o arquivo pronto para recebê-lo.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Módulo de preferências da sidebar, com teste</name>
  <files>src/renderer/src/lib/sidebar-preferences.ts, src/renderer/src/lib/sidebar-preferences.test.ts</files>
  <action>
    Criar `sidebar-preferences.ts` copiando a ESTRUTURA de
    `screenshare-preferences.ts` (mesmo formato de `STORAGE_KEY`, mesmo
    `sanitize` defensivo, mesmo try/catch que nunca deixa storage indisponível
    explodir):
    ```ts
    export type SidebarPreferences = {
      /** Seções recolhidas por tipo de canal. Ausente = expandida. */
      textCollapsed: boolean
      voiceCollapsed: boolean
    }
    ```
    - `STORAGE_KEY = 'janja:sidebar-preferences'` (mesmo prefixo dos outros dois).
    - `DEFAULT_SIDEBAR_PREFERENCES = { textCollapsed: false, voiceCollapsed: false }`
      — tudo expandido no primeiro uso: recolher é uma escolha, nunca o padrão.
    - `loadSidebarPreferences()` e `saveSidebarPreferences(prefs)`.
    - Comentário no topo explicando que é estado de MÁQUINA, não de conta
      (mesma justificativa dos outros dois módulos: a preferência não deve
      viajar junto com a conta para outro computador), e que por isso NÃO vai
      para o Convex.

    Criar o teste no mesmo molde do `screenshare-preferences.test.ts`
    (ambiente `edge-runtime` padrão, sem docblock de jsdom — é função pura +
    stub de storage):
    1. Sem `localStorage` disponível → `load` devolve o default e `save` não lança.
    2. Round-trip: o que foi salvo é o que volta a ser lido.
    3. JSON corrompido / tipo errado → devolve o default, não lança.
  </action>
  <verify>`npx vitest run src/renderer/src/lib/sidebar-preferences.test.ts` passa; `npm run typecheck` limpo.</verify>
  <done>Existe um módulo de persistência testado, no mesmo padrão dos outros dois do projeto.</done>
</task>

<task type="auto">
  <name>Task 2: TEXTO e VOZ viram Collapsible</name>
  <files>src/renderer/src/components/shell/ChannelSidebar.tsx</files>
  <action>
    Substituir os dois blocos `<div className="px-3 pb-1 text-xs font-semibold
    uppercase text-muted-foreground">TEXTO|VOZ</div>` + lista por
    `Collapsible`:
    - `CollapsibleTrigger` é um `<button>` com o rótulo da seção e um chevron
      (`ChevronRight` girando 90° quando aberto, OU `ChevronDown`/`ChevronRight`
      alternados — `lucide-react` já tem os dois, nenhum ícone novo a instalar).
      O gatilho precisa ser focável e ter o rótulo como texto (o Radix já
      fornece `aria-expanded`; não duplicar à mão).
    - `CollapsibleContent` envolve a lista de canais já existente. NÃO mudar
      `TextChannelRow`/`VoiceChannelRow` além do que a Task 3 pede.
    - Acrescentar a contagem de canais no rótulo? NÃO — o Discord não faz, e o
      número muda a cada canal criado, virando ruído. Só rótulo + chevron.
    - Estado inicial vem de `loadSidebarPreferences()` (uma vez, com
      `useState(() => ...)` para não reler a cada render); cada alternância
      atualiza o estado e chama `saveSidebarPreferences`.
    - O `<Separator className="my-2" />` entre as duas seções continua, e
      continua condicionado a existirem canais dos dois tipos.
    - Caso de borda a respeitar: com a seção VOZ recolhida, os participantes de
      voz aninhados somem junto — isso é o comportamento esperado (é o conteúdo
      da seção), mas quem está CONECTADO a um canal de voz perde a referência
      visual. Manter a barra de voz do rodapé (`VoiceControlBar`) como está —
      ela já mostra "Conectado a {canal}" e é o que cobre esse caso. Registrar
      isso no SUMMARY em vez de inventar um estado especial.
  </action>
  <verify>
    `grep -n "Collapsible" src/renderer/src/components/shell/ChannelSidebar.tsx` retorna trigger e content das duas seções;
    `npm run build` passa;
    `npx vitest run` continua verde.
  </verify>
  <done>As duas seções recolhem/expandem, o gatilho é focável, e o estado é lido e gravado pelo módulo da Task 1.</done>
</task>

<task type="auto">
  <name>Task 3: Sidebar nas convenções — tokens, destaque do canal selecionado e cn()</name>
  <files>src/renderer/src/components/shell/ChannelSidebar.tsx</files>
  <action>
    1. Cor direta → token (valores do Plano 08.5-01):
       - linha ~264, anel de participante falando: `ring-2 ring-green-500` → `ring-2 ring-success`
       - linha ~275, ícone de compartilhamento: `text-green-500` → `text-success`
    2. Destaque do canal selecionado. Hoje selecionado = `bg-accent
       text-accent-foreground`, que num tema monocromático é só "um cinza a
       mais". Acrescentar um marcador em `bg-highlight`: uma barra vertical
       fina (3px, arredondada) à esquerda da linha selecionada, no MESMO
       vocabulário visual do indicador de servidor ativo do rail. Manter o
       `bg-accent` — o destaque complementa, não substitui.
       Regra da fase (CONTEXT/decisões do usuário): o tom de destaque só
       aparece em estado ativo/selecionado, anel de foco e marcador de
       não-lido. Nada de pintar ícone ou texto de canal com ele.
    3. Badge de não lidas (`TextChannelRow`): trocar `variant="secondary"` por
       o destaque (`className="bg-highlight text-highlight-foreground"` ou
       equivalente que respeite o componente `Badge` instalado) — "marcador de
       não-lido" é um dos três usos autorizados do destaque, e hoje o badge
       cinza se perde no fundo cinza.
    4. `cn()` onde houver concatenação condicional de string
       (`isSpeaking ? 'ring-2 ring-success' : undefined` já é aceitável, mas
       qualquer `'classe ' + (cond ? 'a' : 'b')` vira `cn()`).
    5. `truncate`/`min-w-0` já existem nas linhas de canal — conferir que
       continuam depois do Collapsible, porque a coluna tem 240px fixos e nome
       de canal longo é o primeiro a estourar.
  </action>
  <verify>
    `grep -nE "green-500|amber-500|red-500" src/renderer/src/components/shell/ChannelSidebar.tsx` vazio;
    `grep -n "highlight" src/renderer/src/components/shell/ChannelSidebar.tsx` retorna o marcador de selecionado e o badge de não lidas;
    `npm run build` passa.
  </verify>
  <done>A sidebar não tem cor direta, o canal selecionado e o badge de não lidas usam o tom de destaque, e classe condicional passa por `cn()`.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` limpo; `npm run build` passa.
- `npx vitest run`: arquivos anteriores + `sidebar-preferences.test.ts`.
- Nenhuma cor direta em `ChannelSidebar.tsx`.
- Nenhum `DropdownMenu` neste arquivo ainda — isso é do Plano 08.5-12, onda 4.

**O que NÃO é verificável neste ambiente (WSL2):**
- Que a animação de recolher é suave e que o chevron gira. Sem tela.
- Que a persistência funciona de verdade entre dois boots do app — o teste
  prova o contrato de `load`/`save` (o observável aqui), não o reinício.
  Item do roteiro do Plano 08.5-15: recolher VOZ, fechar o app, reabrir,
  continuar recolhido.
- Que a barra de destaque do canal selecionado é visível contra o fundo.
</verification>

<success_criteria>
TEXTO e VOZ recolhem, o estado sobrevive ao reinício (contrato testado), o
canal selecionado e as não lidas usam o tom de destaque, e não resta cor direta
no arquivo.
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/08.5-repaginacao-da-ui/08.5-08-SUMMARY.md`.
Registrar o estado final do arquivo (o Plano 08.5-12 vai editar o MESMO arquivo
logo depois e precisa saber onde ficaram o cabeçalho do servidor e as linhas de
canal).
</output>
