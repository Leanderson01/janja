---
phase: 08.5-repaginacao-da-ui
plan: 09
type: execute
wave: 4
depends_on: ["08.5-02", "08.5-03"]
files_modified:
  - src/renderer/src/components/shell/VoiceControlBar.tsx
  - src/renderer/src/features/auth/UserPanel.tsx
  - src/renderer/src/components/shell/MicTestPanel.tsx
  - src/renderer/src/components/shell/VoiceSettingsPopover.tsx
autonomous: true

must_haves:
  truths:
    - "O rodapé da sidebar (240px de largura) cabe sem os controles se empilharem ou serem cortados, tanto conectado quanto desconectado"
    - "O painel do usuário abre um menu por clique e por teclado, com sair da conta dentro dele"
    - "Reconectando aparece em cor de aviso e compartilhando em cor de sucesso, ambos por token semântico"
    - "Nenhum space-x/space-y resta nos dois painéis de configuração de voz"
  artifacts:
    - path: "src/renderer/src/features/auth/UserPanel.tsx"
      provides: "Painel do usuário como gatilho de DropdownMenu, com AUTH-05 (sair da conta) preservado dentro do menu"
      contains: "DropdownMenu"
    - path: "src/renderer/src/components/shell/VoiceControlBar.tsx"
      provides: "Barra de voz com tokens semânticos e texto de status que não rouba espaço quando não há conexão"
      contains: "text-warning"
  key_links:
    - from: "src/renderer/src/features/auth/UserPanel.tsx"
      to: "useAuth().signOut"
      via: "item de menu Sair da conta — AUTH-05 continua alcançável pela interface, só mudou de lugar"
      pattern: "signOut"
---

<objective>
Reavaliar o rodapé da sidebar, que hoje empilha DUAS barras fixas
(`VoiceControlBar` de 56px + `UserPanel` de ~56-64px) numa coluna de 240px, e
converter o painel do usuário no `DropdownMenu` que o brief pede — liberando o
botão solto de sair da conta.

Purpose: o CONTEXT.md pede explicitamente reavaliar o rodapé, porque o toggle
de qualidade da F8 já foi parar num popover por falta de espaço. O RESEARCH
mediu: ~120px de altura fixa no fundo da coluna antes de qualquer conteúdo, e
5 controles + texto de status disputando 240px de largura.
Output: rodapé que não estoura em largura, menu do usuário funcionando por
teclado, tokens semânticos e `gap` no lugar de `space-y`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.5-repaginacao-da-ui/08.5-RESEARCH.md
@.planning/phases/08.5-repaginacao-da-ui/08.5-01-SUMMARY.md
@.planning/phases/08.5-repaginacao-da-ui/08.5-02-SUMMARY.md
@src/renderer/src/components/shell/VoiceControlBar.tsx
@src/renderer/src/features/auth/UserPanel.tsx

# ORDEM MUDOU (2026-08-19): este plano saiu da onda 3 para a onda 4 porque o
# Plano 08.5-03 (palco da call) edita `VoiceControlBar.tsx` antes — o texto
# "Conectado a {canal}" virou um BOTÃO que volta para o palco da call. Ao
# aplicar a Task 1 (esconder o texto quando desconectado), preservar esse
# botão no estado conectado: ele é um dos dois caminhos de volta ao palco.
# Reler `08.5-03-SUMMARY.md` e o arquivo real antes de editar.
#
# AVISO para a onda seguinte: o Plano 08.5-11 (VOICE-18) vai REMOVER deste
# arquivo o efeito que aplica `track.setVolume(deafened ? 0 : 1)`, movendo-o
# para `voice-context.tsx`. Não "melhorar" esse efeito aqui — ele está de
# saída.
#
# Propriedade de arquivo: `ChannelSidebar.tsx` (que RENDERIZA estes dois
# componentes no rodapé) é do Plano 08.5-08 nesta mesma onda — NÃO editar.
# Se a mudança de rodapé exigir tocar na sidebar, parar e registrar no SUMMARY
# em vez de editar: o Plano 08.5-12 (onda 4) é quem pode absorver isso.
#
# NÃO mexer em nenhuma lógica de voz: mute, deafen, VAD, push-to-talk,
# compartilhamento e o testador de microfone são F7/F8 e estão com
# verificação humana PENDENTE (07-08, 08-03, 08-07, quick/001). Esta fase é
# de acabamento visual — mudar comportamento de mídia aqui contaminaria
# aqueles checkpoints com uma variável nova.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Barra de voz — tokens semânticos e largura sob controle</name>
  <files>src/renderer/src/components/shell/VoiceControlBar.tsx</files>
  <action>
    1. Cor direta → token (Plano 08.5-01):
       - linha ~221: `text-amber-500` ("Reconectando...") → `text-warning`
       - linha ~274: `text-green-500` (ícone de compartilhamento ativo) → `text-success`
       - `text-destructive` em mute/deafen/desconectar já está certo: não mexer.
    2. Largura: hoje o bloco de status ocupa `flex-1` com o texto "Não conectado
       a nenhum canal de voz" mesmo quando não há conexão nenhuma, roubando
       espaço de 3 a 5 botões numa coluna de 240px. Mudar para:
       - **Desconectado** (`!hasIntention`): não renderizar o texto longo. Os
         botões (mic, fones, configurações) ficam alinhados e o espaço sobra.
         O estado "desconectado" já é evidente pela ausência do botão de
         desconectar e pelo estado dos ícones.
       - **Conectado/conectando/reconectando**: manter o texto, com
         `truncate` e `min-w-0` (o nome do canal entra nele), como já está.
       Deixar comentário registrando a decisão — ela existe por espaço, não
       por estética.
    3. `cn()` para qualquer classe condicional montada por ternário de string.
    4. NÃO mover controles para dentro do popover nem tirar de lá: a
       distribuição atual (mute/deafen/compartilhar/configurações/sair na
       barra; qualidade e dispositivos no popover) fica como está. Mexer nisso
       agora atrapalharia os checkpoints de voz ainda pendentes.
  </action>
  <verify>`grep -nE "green-500|amber-500|red-500" src/renderer/src/components/shell/VoiceControlBar.tsx` vazio; `npm run build` passa; `npx vitest run` verde.</verify>
  <done>A barra de voz usa só tokens semânticos e não gasta largura com texto quando não há conexão.</done>
</task>

<task type="auto">
  <name>Task 2: Painel do usuário vira DropdownMenu</name>
  <files>src/renderer/src/features/auth/UserPanel.tsx</files>
  <action>
    Converter a linha inteira (avatar + nome + e-mail) no gatilho de um
    `DropdownMenu`, no mesmo padrão único de menu da fase (Plano 08.5-02):
    - `<DropdownMenuTrigger asChild>` recebe um `<button type="button" className="w-full ...">`
      com o conteúdo atual da linha; manter `truncate` no nome e no e-mail
      (240px de coluna, e-mail longo é garantido).
    - `onContextMenu` abre o mesmo menu (menu controlado, igual ao do membro).
    - Itens:
      1. **"Copiar e-mail"** → `navigator.clipboard.writeText(user.email)` +
         `toast.success('E-mail copiado')`.
      2. Separador.
      3. **"Sair da conta"** → `void signOut()`, com estilo destrutivo
         (`text-destructive`, ou `variant="destructive"` se o
         `DropdownMenuItem` instalado expuser). **AUTH-05 depende deste item**:
         o requisito é "usuário consegue sair da conta pelo app", e antes do
         Plano 02-08 isso só existia no console do DevTools. Sair não pode
         ficar inalcançável.
    - Remover o botão solto de sair (e seu `Tooltip`) do rodapé, já que a ação
      passou para dentro do menu — é o ganho de largura desta task.
    - **NÃO** incluir "Copiar meu identificador USER#123": o hook que resolve
      username/tag (`useMyIdentifier`) vive dentro de `FriendsPanel.tsx`, que
      pertence ao Plano 08.5-06 nesta mesma onda. Extrair o hook agora
      significaria dois planos editando o mesmo arquivo em paralelo — o defeito
      que a lição nº 6 do HANDOFF.md registra. O identificador continua
      copiável no painel de amigos, onde já está. Registrar no SUMMARY como
      candidato barato para depois.
    - Manter `if (!user) return null` como está.
  </action>
  <verify>`grep -n "DropdownMenu" src/renderer/src/features/auth/UserPanel.tsx` retorna o menu; `grep -n "signOut" ...` continua existindo; `npm run typecheck` limpo; `npm run build` passa.</verify>
  <done>O painel do usuário abre menu por clique/Enter/botão direito, e sair da conta continua alcançável — agora de dentro do menu.</done>
</task>

<task type="auto">
  <name>Task 3: gap no lugar de space-x/space-y nos painéis de voz</name>
  <files>src/renderer/src/components/shell/MicTestPanel.tsx, src/renderer/src/components/shell/VoiceSettingsPopover.tsx</files>
  <action>
    Converter as 10-11 ocorrências de `space-x-*`/`space-y-*` mapeadas pelo
    RESEARCH (`MicTestPanel.tsx:209,210,226,243,262,278` e
    `VoiceSettingsPopover.tsx:258,290,334,360,381,400`) para `gap-*` — o que
    exige que o container seja flex ou grid. Regra da conversão:
    - `space-y-N` num container vertical → `flex flex-col gap-N`.
    - `space-x-N` num container horizontal → `flex items-center gap-N`
      (preservando o alinhamento que já existia).
    - Se algum container NÃO puder virar flex sem mudar o layout (ex. depende
      de `display: block` para algum filho), deixar como está e registrar a
      exceção em comentário — a convenção existe para consistência, não para
      quebrar layout que já funciona.

    Verificar depois da conversão que nada mais no arquivo depende do seletor
    `> * + *` do `space-*` (ex. um filho condicional que sumia e voltava).

    NÃO mudar rótulos, ordem de controles, valores padrão ou qualquer lógica —
    o medidor de nível, o slider de VAD e o testador de microfone continuam
    idênticos. `08.5` não é a fase que mexe em voz.
  </action>
  <verify>`grep -rnE "space-[xy]-" src/renderer/src/components/shell/MicTestPanel.tsx src/renderer/src/components/shell/VoiceSettingsPopover.tsx` vazio (ou só as exceções documentadas); `npm run build` passa; `git diff` não mostra mudança em nenhuma chamada de função de voz.</verify>
  <done>Os dois painéis usam `gap`, sem nenhuma alteração de comportamento.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` limpo; `npm run build` passa; `npx vitest run` verde (235+).
- `grep -rnE "green-500|amber-500" src/renderer/src/components/shell/VoiceControlBar.tsx` vazio.
- `git diff src/renderer/src/state/voice-context.tsx src/renderer/src/lib/` vazio — nenhuma lógica de voz foi tocada.

**O que NÃO é verificável neste ambiente (WSL2, sem áudio e sem tela):**
- Que o rodapé cabe em 240px sem quebrar linha. É medida de pixel — checkpoint
  humano (Plano 08.5-17), com atenção ao estado CONECTADO, que é o que tem
  mais botões visíveis.
- Que o menu do usuário abre para cima (a linha fica colada no fim da janela)
  em vez de sair da tela. O Radix reposiciona sozinho, mas isso é
  posicionamento real, que jsdom não calcula.
- Nada de mute/deafen/VAD/compartilhamento — sem dispositivo de áudio aqui, e
  esses caminhos já têm checkpoints próprios pendentes (07-08, 08-03, 08-07).
</verification>

<success_criteria>
O rodapé perdeu um botão solto e ganhou um menu de usuário navegável por
teclado; nenhuma cor direta e nenhum `space-*` resta nos quatro arquivos; nada
do comportamento de voz mudou.
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/08.5-repaginacao-da-ui/08.5-09-SUMMARY.md`,
registrando as exceções de `space-*` que não puderam virar `gap` (se houver) e
a decisão sobre "copiar identificador" no menu do usuário.
</output>
