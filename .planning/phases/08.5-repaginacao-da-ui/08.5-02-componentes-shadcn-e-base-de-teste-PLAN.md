---
phase: 08.5-repaginacao-da-ui
plan: 02
type: execute
wave: 2
depends_on: ["08.5-01"]
files_modified:
  - src/renderer/src/components/ui/collapsible.tsx
  - src/renderer/src/components/ui/dropdown-menu.tsx
  - src/renderer/src/components/ui/sonner.tsx
  - src/renderer/src/components/shell/AppShell.tsx
  - src/renderer/src/test/jsdom-setup.ts
  - src/renderer/src/components/ui/dropdown-menu.test.tsx
  - package.json
  - package-lock.json
autonomous: true

must_haves:
  truths:
    - "Collapsible, DropdownMenu e Sonner existem em src/renderer/src/components/ui/ e compilam — nenhum arquivo ficou no diretório espúrio ./@/ da raiz"
    - "next-themes NÃO está em package.json: o wrapper do Toaster foi simplificado para tema escuro fixo"
    - "Um <Toaster /> único está montado no shell e qualquer componente pode chamar toast() de sonner"
    - "É possível escrever teste de componente React neste projeto sem tocar na config global do vitest, e os 235 testes existentes continuam passando no ambiente edge-runtime"
    - "Um menu do Radix abre por teclado, navega por seta, fecha com Escape e devolve o foco ao gatilho — provado por teste automatizado, não por leitura de código"
  artifacts:
    - path: "src/renderer/src/components/ui/dropdown-menu.tsx"
      provides: "Primitivo de menu do Radix já estilizado no padrão new-york, consumido pelos planos 05, 06, 07 e 09"
      min_lines: 40
    - path: "src/renderer/src/components/ui/sonner.tsx"
      provides: "Wrapper do Toaster sem next-themes, com theme fixo em dark"
      contains: "sonner"
    - path: "src/renderer/src/test/jsdom-setup.ts"
      provides: "Polyfills mínimos que o Radix precisa e o jsdom não tem (hasPointerCapture, scrollIntoView, ResizeObserver)"
      contains: "hasPointerCapture"
    - path: "src/renderer/src/components/ui/dropdown-menu.test.tsx"
      provides: "Prova executável de que o par jsdom + testing-library funciona neste projeto e de que o comportamento de teclado do Radix é observável aqui"
      contains: "@vitest-environment jsdom"
  key_links:
    - from: "src/renderer/src/components/shell/AppShell.tsx"
      to: "src/renderer/src/components/ui/sonner.tsx"
      via: "<Toaster /> montado uma única vez dentro do TooltipProvider"
      pattern: "Toaster"
    - from: "src/renderer/src/components/ui/dropdown-menu.test.tsx"
      to: "src/renderer/src/test/jsdom-setup.ts"
      via: "import do setup no topo do arquivo de teste, antes de renderizar qualquer componente Radix"
      pattern: "jsdom-setup"
---

<objective>
Instalar as peças que os planos de UI da onda 3 e 4 consomem, e provar que elas
funcionam aqui — antes de seis planos paralelos assumirem que funcionam. São
três instalações com armadilha conhecida: o CLI do shadcn escreve fora do lugar
neste repo, o template do Sonner importa uma lib que não deve existir aqui, e o
projeto nunca renderizou um componente React em teste.

Purpose: concentrar num único plano tudo que mexe em `package.json` /
`package-lock.json` e em `components/ui/`. Se isso ficasse espalhado, dois
planos paralelos escreveriam o mesmo lockfile — e um `npm install` concorrente
não dá merge conflict, dá lockfile silenciosamente errado.
Output: `Collapsible`, `DropdownMenu` e `toast()` disponíveis; um teste de
componente rodando de verdade, com os 235 testes existentes intocados.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.5-repaginacao-da-ui/08.5-RESEARCH.md
@.planning/phases/08.5-repaginacao-da-ui/08.5-01-SUMMARY.md
@src/renderer/src/components/shell/AppShell.tsx
@components.json
@vitest.config.ts
@package.json

# Decisões de escopo já tomadas no planejamento — não reabrir:
#
# 1. `sheet` NÃO é instalado. O CONTEXT.md condiciona: "Sheet só se ganhar uso
#    real em janela estreita, não por imitação de layout mobile". Nenhum plano
#    desta fase usa Sheet: as colunas continuam fixas (decisão da F3) e a
#    lista de membros vira alternável por botão (Plano 08.5-05), não gaveta.
#    Instalar um componente sem consumidor é dívida, não preparo.
#
# 2. `context-menu` NÃO é instalado. Os menus de servidor, canal, membro e
#    usuário desta fase usam UM padrão só: `DropdownMenu` controlado, aberto
#    por clique/Enter no gatilho E por `onContextMenu` (botão direito) no mesmo
#    elemento. Motivo: `ContextMenu` do Radix é alcançável só por mouse
#    (não tem gatilho focável), então usá-lo obrigaria a manter DUAS
#    implementações do mesmo menu para satisfazer navegação por teclado —
#    exatamente o tipo de menu que "parece funcionar e não funciona no
#    teclado" que o RESEARCH lista em Don't Hand-Roll. Custo aceito: o menu
#    ancora no elemento, não no cursor.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar Collapsible e DropdownMenu, corrigindo o destino dos arquivos</name>
  <files>src/renderer/src/components/ui/collapsible.tsx, src/renderer/src/components/ui/dropdown-menu.tsx</files>
  <action>
    Rodar, da raiz do repo:
    ```
    npx shadcn@4.18.0 add collapsible dropdown-menu
    ```
    Versão pinada, não `@latest`: os 12 componentes já instalados vieram do
    template da 4.18.0 e misturar versões de template gera divergência de
    estilo silenciosa (`08.5-RESEARCH.md`, Standard Stack).

    **NÃO passar `--overwrite`, `--force` nem qualquer auto-confirmação cega**
    (HANDOFF.md lição nº 7 — um `yes |` já custou um incidente de perda de
    dados neste repo). Se o CLI perguntar se pode sobrescrever algum dos 12
    componentes existentes, a resposta é NÃO: nenhum deles precisa mudar aqui.

    **Pitfall 1 do RESEARCH, já visto duas vezes neste projeto (Plano 03-01):**
    o CLI não acha `vite.config.ts` na raiz (só existe `electron.vite.config.ts`)
    e escreve num diretório literal `./@/components/ui/` em vez de resolver o
    alias. Procedimento obrigatório depois do `add`:
    1. `ls -la @/ 2>/dev/null` e `git status --short`.
    2. Se existir `./@/components/ui/*.tsx`: mover os arquivos para
       `src/renderer/src/components/ui/`, conferir que `./@/` ficou vazio
       (`find @ -type f`) e só então `rm -rf @`.
    3. Se os arquivos já caíram no lugar certo, seguir sem mover nada.
    4. Conferir que os imports internos dos arquivos novos usam `@/lib/utils`
       (alias do projeto) e que nenhum dos 12 componentes antigos aparece como
       modificado em `git status --short`.

    Nem `collapsible` nem `dropdown-menu` exigem `npm install` novo — os dois
    primitivos já vêm no pacote consolidado `radix-ui@1.6.7`, confirmado no
    RESEARCH pelos re-exports em `node_modules/radix-ui/dist/index.d.ts`. Se o
    CLI mesmo assim adicionar `@radix-ui/react-*` avulso ao `package.json`,
    remover a dependência avulsa e ajustar o import do componente para
    `radix-ui`, no mesmo padrão dos 12 já instalados (conferir um deles antes,
    ex. `popover.tsx`, e seguir literalmente o estilo de import de lá).
  </action>
  <verify>
    `ls src/renderer/src/components/ui/` mostra `collapsible.tsx` e `dropdown-menu.tsx`;
    `test ! -d @` (o diretório espúrio não existe);
    `git status --short` não mostra nenhum dos 12 componentes antigos como modificado;
    `npm run typecheck` limpo.
  </verify>
  <done>Os dois componentes existem sob `src/renderer/src/components/ui/`, importam de `radix-ui` e `@/lib/utils`, e o typecheck passa.</done>
</task>

<task type="auto">
  <name>Task 2: Instalar Sonner, arrancar o next-themes do wrapper e montar o Toaster</name>
  <files>src/renderer/src/components/ui/sonner.tsx, src/renderer/src/components/shell/AppShell.tsx, package.json, package-lock.json</files>
  <action>
    Rodar `npx shadcn@4.18.0 add sonner` (mesmas regras da Task 1: sem
    `--overwrite`, conferir `./@/`, mover se preciso).

    Diferente dos outros dois, este traz uma dependência de runtime real
    (`sonner`) — essa deve ficar em `dependencies`. O problema é outro:

    **Pitfall 3 do RESEARCH.** O template oficial do wrapper importa
    `useTheme` de `next-themes`. `next-themes` é uma lib de alternância de
    tema para Next.js; este projeto não é Next, não tem alternância de tema
    (Plano 08.5-01 fixou `class="dark"` no HTML) e não deve ganhar essa
    dependência. Depois do `add`:
    1. Se `next-themes` apareceu em `package.json`: `npm uninstall next-themes`.
    2. Editar `src/renderer/src/components/ui/sonner.tsx`: remover o import de
       `next-themes` e a chamada a `useTheme()`, passando `theme="dark"` fixo
       ao `<Sonner />`. Deixar um comentário curto explicando por quê (tema
       fixo, ver `index.html`) — senão a próxima pessoa "conserta" isso de
       volta.
    3. Conferir com `grep -rn "next-themes" package.json src/` — precisa
       retornar vazio.

    Em `AppShell.tsx`: montar `<Toaster />` UMA vez, dentro do
    `TooltipProvider` e ao lado do `<ScreenSharePicker />` já existente (mesma
    justificativa que está comentada lá: uma instância por app). Configurar
    `position="bottom-right"` e `richColors={false}` — o app inteiro é escuro e
    monocromático, e cores próprias do sonner brigariam com os tokens do
    Plano 08.5-01. Acrescentar comentário registrando que os toasts são o
    canal de feedback padrão da fase (envio falho, ação de menu concluída),
    substituindo os `.catch(() => {})` mudos que os planos 03 e 08 vão remover.

    NÃO alterar mais nada em `AppShell.tsx` — o Plano 08.5-05 é dono deste
    arquivo na onda seguinte (alternância da lista de membros) e vai reler o
    que este plano deixou.
  </action>
  <verify>
    `grep -rn "next-themes" package.json src/` retorna vazio;
    `grep -n "\"sonner\"" package.json` mostra a dependência em `dependencies`;
    `grep -n "Toaster" src/renderer/src/components/shell/AppShell.tsx` retorna uma única montagem;
    `npm run typecheck` e `npm run build` passam.
  </verify>
  <done>`toast()` de `sonner` pode ser chamado de qualquer componente do renderer, o Toaster está montado uma vez, e `next-themes` não existe no projeto.</done>
</task>

<task type="auto">
  <name>Task 3: Base de teste de componente (jsdom + testing-library) provada com um menu real</name>
  <files>src/renderer/src/test/jsdom-setup.ts, src/renderer/src/components/ui/dropdown-menu.test.tsx, package.json, package-lock.json</files>
  <action>
    ```
    npm install -D jsdom @testing-library/react @testing-library/dom @testing-library/user-event
    ```
    `@testing-library/dom` entra explicitamente porque a v16 do
    `@testing-library/react` (a que suporta React 19) declara o DOM como
    peer dependency, não como dependência própria. `@testing-library/jest-dom`
    NÃO entra: as asserções deste projeto vão usar DOM puro
    (`document.activeElement`, `getAttribute`), o que evita um quinto pacote e
    um arquivo de setup global.

    **Não tocar em `vitest.config.ts`.** O ambiente global continua
    `edge-runtime` — é o que `convex-test` precisa e o que os 18 arquivos de
    teste atuais usam. O ambiente por arquivo vem do docblock
    `// @vitest-environment jsdom` na primeira linha do arquivo de teste
    (suportado pelo vitest 1.6, instalado).

    Criar `src/renderer/src/test/jsdom-setup.ts` com os polyfills que o Radix
    usa e o jsdom não implementa — sem eles o teste falha com erro obscuro de
    `hasPointerCapture is not a function`, que é exatamente o tipo de
    obstáculo que faria um plano posterior desistir de testar teclado:
    ```ts
    // Importado no topo de todo arquivo de teste que renderiza componente Radix.
    if (!Element.prototype.hasPointerCapture) {
      Element.prototype.hasPointerCapture = (): boolean => false
      Element.prototype.setPointerCapture = (): void => {}
      Element.prototype.releasePointerCapture = (): void => {}
    }
    if (!Element.prototype.scrollIntoView) {
      Element.prototype.scrollIntoView = (): void => {}
    }
    globalThis.ResizeObserver ??= class {
      observe(): void {}
      unobserve(): void {}
      disconnect(): void {}
    } as unknown as typeof ResizeObserver
    ```
    (ajustar a tipagem ao que o `typecheck:web` aceitar — o arquivo precisa
    passar em `npm run typecheck`, que inclui `src/renderer/src/**/*`.)

    Criar `src/renderer/src/components/ui/dropdown-menu.test.tsx`, com
    `// @vitest-environment jsdom` na primeira linha e import do setup logo
    depois. Renderizar um `DropdownMenu` mínimo (gatilho `<button>Abrir</button>`
    + dois itens) e provar, com `userEvent` e teclado — nunca com clique de
    mouse, porque teclado é justamente o que precisa de prova:
    1. `userEvent.tab()` leva o foco ao gatilho.
    2. `{Enter}` abre o menu (existe item com `role="menuitem"` visível).
    3. `{ArrowDown}` move o item ativo (o segundo item vira
       `document.activeElement`, ou `data-highlighted` muda — usar o que o
       Radix instalado de fato expõe; verificar rodando, não supor).
    4. `{Escape}` fecha o menu e o foco VOLTA ao gatilho
       (`expect(document.activeElement).toBe(trigger)`).

    Se algum destes 4 comportamentos não for observável em jsdom, NÃO forçar:
    remover a asserção, deixar comentário no arquivo dizendo o que não é
    observável aqui, e registrar no SUMMARY — os planos 05 e 09 precisam saber
    o que dá para provar antes de prometerem prova. Ficar com um teste menor e
    honesto é melhor que um teste que passa por acidente.
  </action>
  <verify>
    `npx vitest run` passa com 19 arquivos de teste (18 antigos + 1 novo) e >= 235 testes;
    nenhum arquivo antigo mudou de ambiente (os testes de `convex/` continuam verdes, que é o sinal de que `vitest.config.ts` ficou intocado);
    `git diff --stat vitest.config.ts` vazio;
    `npm run typecheck` limpo (inclui os arquivos de teste, porque `tsconfig.web.json` cobre `src/renderer/src/**/*`).
  </verify>
  <done>Existe uma suíte de teste de componente rodando, com polyfills isolados num único arquivo, sem mudança de configuração global e sem regressão nos 235 testes.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` limpo nos três projetos.
- `npm run build` passa.
- `npx vitest run`: 19 arquivos, >= 235 testes, nenhum falhando.
- `test ! -d @` — o diretório espúrio do CLI do shadcn não sobreviveu.
- `grep -rn "next-themes" package.json src/` vazio.
- `git status --short` não mostra os 12 componentes de UI anteriores como modificados.

**O que NÃO é verificável neste ambiente:**
- Que um toast aparece na tela e some sozinho. jsdom não pinta e WSL2 não
  renderiza janela — a prova é o roteiro do Plano 08.5-15 em Windows.
- Que a animação do Collapsible é suave. Mesma razão.
- jsdom prova COMPORTAMENTO de componente (foco, ARIA, teclado dentro de uma
  região). Não prova a janela inteira, nem foco visível de verdade, nem
  navegação por Tab entre as 4 regiões — isso é do checkpoint humano
  (HANDOFF.md lição nº 1: verificar no ambiente errado não é verificar).
</verification>

<success_criteria>
Os planos 03 a 09 podem importar `Collapsible`, `DropdownMenu` e `toast` sem
instalar nada, e podem escrever teste de teclado com um padrão já provado
funcionando neste repo. Nenhuma dependência morta (`next-themes`, `sheet`,
`context-menu`) entrou junto.
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/08.5-repaginacao-da-ui/08.5-02-SUMMARY.md`.
Registrar obrigatoriamente: (a) se o CLI escreveu em `./@/` de novo, (b) quais
dos 4 comportamentos de teclado o jsdom conseguiu observar e quais não, (c) a
lista final de dependências adicionadas. Os planos 05 e 09 leem esse SUMMARY
antes de escrever os próprios testes.
</output>
