---
phase: 08.5-repaginacao-da-ui
plan: 12
type: execute
wave: 5
depends_on: ["08.5-08"]
files_modified:
  - src/renderer/src/components/shell/ChannelSidebar.tsx
  - src/renderer/src/components/shell/ChannelSidebar.test.tsx
autonomous: true

must_haves:
  truths:
    - "O nome do servidor abre um menu com convidar e criar canal, alcançável por Tab e Enter"
    - "Cada canal tem um menu com ações que existem hoje, alcançável por teclado e por botão direito"
    - "Os dois diálogos que já existiam (convite e criar canal) continuam funcionando, agora abertos pelo menu"
    - "O cabeçalho da sidebar deixa de gastar largura com dois botões de ícone numa coluna de 240px"
  artifacts:
    - path: "src/renderer/src/components/shell/ChannelSidebar.tsx"
      provides: "Menu do servidor no cabeçalho e menu por canal, ambos DropdownMenu controlado"
      contains: "DropdownMenu"
    - path: "src/renderer/src/components/shell/ChannelSidebar.test.tsx"
      provides: "Prova de teclado do menu de canal: Tab alcança o gatilho, Enter abre, Escape fecha e devolve o foco"
      contains: "@vitest-environment jsdom"
  key_links:
    - from: "src/renderer/src/components/shell/ChannelSidebar.tsx"
      to: "convex/channelReadState.ts:openChannel"
      via: "item de menu Marcar como lido chama a mutation existente"
      pattern: "openChannel"
---

<objective>
Acrescentar os menus de servidor e de canal, o último item do grupo "Navegação
e menus" do CONTEXT.md.

Purpose: **este plano existe numa onda separada por colisão de arquivo, não por
dependência lógica.** O Plano 08.5-08 (onda 3) reescreve as seções de
`ChannelSidebar.tsx` para `Collapsible`; se os menus fossem escritos em
paralelo, dois agentes editariam as mesmas linhas do mesmo arquivo — o defeito
registrado na lição nº 6 do HANDOFF.md, que já custou uma entrada inteira do
ROADMAP.md. Executar depois custa uma onda; executar junto custa o arquivo.
Output: menu de servidor, menu de canal, teste de teclado, e um cabeçalho de
sidebar mais estreito.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.5-repaginacao-da-ui/08.5-RESEARCH.md
@.planning/phases/08.5-repaginacao-da-ui/08.5-02-SUMMARY.md
@.planning/phases/08.5-repaginacao-da-ui/08.5-08-SUMMARY.md
@.planning/phases/08.5-repaginacao-da-ui/08.5-04-SUMMARY.md
@src/renderer/src/components/shell/ChannelSidebar.tsx
@convex/channels.ts
@convex/channelReadState.ts

# LER PRIMEIRO o 08.5-08-SUMMARY.md e o arquivo real: o Plano 08.5-08 acabou
# de reestruturar este arquivo (Collapsible, tokens, marcador de selecionado).
# Não assumir o conteúdo anterior.
#
# O 08.5-04-SUMMARY.md está no contexto porque o menu do MEMBRO já foi
# construído lá com o mesmo padrão (DropdownMenu controlado + onContextMenu) e
# já registrou o que o jsdom consegue provar. Copiar o padrão, não reinventar.
#
# Não existe mutation de apagar canal, renomear canal, sair do servidor ou
# apagar servidor em `convex/`. Menu não inventa capacidade: item que não tem
# backend não entra (criar essas mutations seria mexer em autorização, o que
# esta fase não faz).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Menu do servidor no cabeçalho da sidebar</name>
  <files>src/renderer/src/components/shell/ChannelSidebar.tsx</files>
  <action>
    Hoje o cabeçalho tem: nome do servidor + dois `Button size="icon-sm"`
    (Convidar, Criar canal). Substituir por um `DropdownMenu` controlado cujo
    gatilho é o próprio nome do servidor:
    - `<DropdownMenuTrigger asChild>` recebe um `<button type="button"
      className="flex min-w-0 flex-1 items-center gap-1 ...">` com o nome
      (`truncate`) e um `ChevronDown` (`size-4 shrink-0`) à direita.
    - `aria-label` explicando a ação ("Menu do servidor {nome}"); o Radix já
      cuida de `aria-expanded`/`aria-haspopup`.
    - `onContextMenu` abre o mesmo menu (padrão único da fase).
    - Itens, todos ligados ao que já existe neste arquivo:
      1. "Convidar pessoas" → `setInviteOpen(true)` (o `InviteDialog` já está
         montado aqui e não muda).
      2. "Criar canal" → `setCreateChannelOpen(true)` (idem `CreateChannelDialog`).
      3. Separador + "Copiar nome do servidor" → clipboard + `toast.success`.
    - Remover os dois botões de ícone e seus `Tooltip`s: o ganho é ~64px de
      largura num cabeçalho de 240px, que é exatamente o aperto que o RESEARCH
      previu para a janela estreita.
    - Os dois `<Dialog>` continuam montados no fim do componente, sem
      alteração — o menu só troca quem chama `setOpen`.
  </action>
  <verify>`grep -n "DropdownMenu" src/renderer/src/components/shell/ChannelSidebar.tsx` retorna o menu do cabeçalho; `grep -c "InviteDialog\|CreateChannelDialog" ...` continua retornando as duas montagens; `npm run build` passa.</verify>
  <done>O nome do servidor abre um menu com as duas ações que já existiam, e os botões de ícone saíram do cabeçalho.</done>
</task>

<task type="auto">
  <name>Task 2: Menu por canal, de texto e de voz</name>
  <files>src/renderer/src/components/shell/ChannelSidebar.tsx</files>
  <action>
    Acrescentar um menu em `TextChannelRow` e em `VoiceChannelRow`. Como as
    linhas já são `<button>` que executam a ação primária (selecionar/entrar),
    o menu NÃO pode roubar o clique esquerdo. Padrão:
    - Um botão "..." (`MoreVertical`, `size-4`) à direita da linha, dentro do
      mesmo container. Visível no hover E sempre que receber foco:
      `opacity-0 group-hover:opacity-100 focus-visible:opacity-100`
      (com `group` no container da linha). Sem o `focus-visible`, o botão fica
      invisível para quem navega por teclado — é o "parece funcionar, não
      funciona no teclado" que o RESEARCH cita.
    - `aria-label` do tipo "Opções do canal {nome}".
    - `onContextMenu` no container da linha abre o MESMO menu.
    - Itens por tipo de canal (só o que tem backend hoje):
      - **Texto**: "Marcar como lido" → `useMutation(api.channelReadState.openChannel)`
        com `{ channelId }`, seguido de `toast.success('Canal marcado como lido')`;
        + "Copiar nome do canal" → clipboard + toast.
      - **Voz**: "Entrar no canal" / "Sair do canal" conforme `isJoined`
        (chama o mesmo handler que a linha já usa — não duplicar lógica de
        junção, chamar `onClick`/`handleVoiceChannelClick`); + "Copiar nome do
        canal".
    - NÃO adicionar apagar/renomear canal, silenciar canal, ou qualquer
      gerenciamento: sem mutation e sem conceito no backend.
    - Cuidado de layout: a linha tem 240px menos indentação; o botão "..."
      precisa de `shrink-0` e o nome do canal precisa manter `truncate`, senão
      canal com nome longo empurra o botão para fora. O badge de não lidas
      (Plano 08.5-08) já disputa esse espaço — conferir a ordem: nome
      (`flex-1 truncate`) → badge (`shrink-0`) → "..." (`shrink-0`).
  </action>
  <verify>`grep -n "MoreVertical" src/renderer/src/components/shell/ChannelSidebar.tsx` retorna o gatilho; `grep -n "openChannel" ...` retorna a mutation; `npm run typecheck` limpo; `npm run build` passa.</verify>
  <done>Canais de texto e de voz têm menu alcançável por teclado e por botão direito, com ações que existem no backend.</done>
</task>

<task type="auto">
  <name>Task 3: Teste de teclado dos menus da sidebar</name>
  <files>src/renderer/src/components/shell/ChannelSidebar.test.tsx</files>
  <action>
    Criar o teste seguindo o padrão já provado (`// @vitest-environment jsdom`,
    import de `@/test/jsdom-setup`, asserções em DOM puro) e o que o
    `08.5-02-SUMMARY.md`/`08.5-04-SUMMARY.md` registraram como observável.

    Testar o componente de LINHA de canal isolado (exportá-lo se necessário) —
    não a `ChannelSidebar` inteira, que depende de 3 `useQuery` do Convex, do
    `useSelection` e do `useVoice`. Casos:
    1. `userEvent.tab()` alcança o botão da linha e, num segundo Tab, o botão
       "..." (prova que o gatilho do menu é focável — o ponto inteiro de não
       usar `ContextMenu` do Radix).
    2. `{Enter}` no "..." abre o menu (há `role="menuitem"` com o texto
       esperado).
    3. `{Escape}` fecha e devolve o foco ao "...".
    4. `fireEvent.contextMenu(linha)` abre o mesmo menu.
    5. Um item do menu, ativado por `{Enter}`, chama o callback esperado
       (passar spy por prop; não montar o Convex).

    Se algum caso não for observável em jsdom, remover e registrar — a regra
    da fase é teste honesto, não teste que passa por acidente (Pitfall 4 do
    RESEARCH: declarar acessibilidade como feita sem rodar nada).
  </action>
  <verify>`npx vitest run src/renderer/src/components/shell/ChannelSidebar.test.tsx` passa; `npx vitest run` inteiro verde; `npm run typecheck` limpo.</verify>
  <done>O comportamento de teclado dos menus da sidebar está provado por execução.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` limpo; `npm run build` passa; `npx vitest run` verde.
- `git diff --stat convex/` vazio — os menus consomem mutations existentes, não criam nenhuma.
- Nenhum item de menu sem handler (grep visual pelos itens: cada `DropdownMenuItem` tem `onSelect`/`onClick`).

**O que NÃO é verificável neste ambiente (WSL2):**
- Que o botão "..." aparece no hover e não some no foco (é pintura + CSS
  `group-hover`, que jsdom não avalia).
- Que o menu não escapa da janela quando o canal está no fim da lista.
- Que "Marcar como lido" zera o badge na prática — exige mensagens reais e o
  Convex do usuário.
Tudo isso entra no roteiro do Plano 08.5-17.
</verification>

<success_criteria>
Servidor e canal têm menu, os dois alcançáveis por teclado, com ações reais; o
cabeçalho da sidebar ficou mais estreito; nenhuma capacidade nova foi inventada
no backend.
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/08.5-repaginacao-da-ui/08.5-12-SUMMARY.md`.
</output>
