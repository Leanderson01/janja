---
phase: 08.5-repaginacao-da-ui
plan: 05
type: execute
wave: 3
depends_on: ["08.5-02"]
files_modified:
  - src/renderer/src/state/layout-context.tsx
  - src/renderer/src/components/shell/AppShell.tsx
  - src/renderer/src/components/shell/ChannelHeader.tsx
  - src/renderer/src/components/shell/ServerRail.tsx
autonomous: true

must_haves:
  truths:
    - "O usuário esconde e mostra a lista de membros por um botão no cabeçalho do canal, e a escolha sobrevive ao reinício"
    - "Com a lista de membros escondida, a área de conversa ocupa o espaço liberado — em janela de 900px isso é a diferença entre ~348px e ~588px de conversa"
    - "O nome do canal trunca em vez de empurrar os botões do cabeçalho para fora"
    - "O servidor ativo no rail é marcado com o tom de destaque da fase"
  artifacts:
    - path: "src/renderer/src/state/layout-context.tsx"
      provides: "Estado de layout da janela (visibilidade da lista de membros), acima de ChannelHeader e AppShell, sem prop drilling por ConversationArea"
      contains: "membersVisible"
    - path: "src/renderer/src/components/shell/ChannelHeader.tsx"
      provides: "Cabeçalho com nome truncado e botão de alternar lista de membros"
      contains: "truncate"
  key_links:
    - from: "src/renderer/src/components/shell/ChannelHeader.tsx"
      to: "src/renderer/src/state/layout-context.tsx"
      via: "botão do cabeçalho chama toggleMembers() do contexto"
      pattern: "useLayout"
    - from: "src/renderer/src/components/shell/AppShell.tsx"
      to: "src/renderer/src/state/layout-context.tsx"
      via: "coluna da lista de membros só é renderizada quando membersVisible"
      pattern: "membersVisible"
---

<objective>
Fechar o item "Janela estreita" do CONTEXT.md: a moldura de 4 regiões continua
com colunas fixas (decisão da F3, não reaberta), mas a área de conversa ganha
uma válvula de escape — a lista de membros passa a ser alternável — e o
cabeçalho para de ser um lugar onde nome de canal longo estoura.

Purpose: o RESEARCH mediu o risco: rail 72 + sidebar 240 + membros 240 = 552px,
sobrando ~348px de conversa numa janela de 900px (o mínimo definido em
`src/main/index.ts:19-20`). O risco entra com esta própria fase, quando botões
novos chegam ao cabeçalho. Alternar a lista de membros é o que o brief pede
para "telas menores", sem inventar layout mobile — que é escopo natimorto aqui
(não existe dispositivo alvo).
Output: botão de membros no cabeçalho, coluna que some e volta, nome truncado,
e o rail marcando o servidor ativo com o tom de destaque.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.5-repaginacao-da-ui/08.5-CONTEXT.md
@.planning/phases/08.5-repaginacao-da-ui/08.5-RESEARCH.md
@.planning/phases/08.5-repaginacao-da-ui/08.5-01-SUMMARY.md
@.planning/phases/08.5-repaginacao-da-ui/08.5-02-SUMMARY.md
@src/renderer/src/components/shell/AppShell.tsx
@src/renderer/src/components/shell/ChannelHeader.tsx
@src/renderer/src/components/shell/ServerRail.tsx
@src/renderer/src/state/selection-context.tsx

# CONTEXTO NOVO (2026-08-19): o Plano 08.5-03, nesta MESMA onda, transforma a
# área principal num palco que alterna entre texto e call. Consequência para
# este plano: `ChannelHeader` passa a ser renderizado só nos modos texto,
# prévia de canal de voz e vazio — no modo palco quem manda é a barra própria
# do `CallStage`. O botão de alternar a lista de membros que esta Task 3
# acrescenta ao `ChannelHeader` ganha um gêmeo na barra do palco, e quem faz
# isso é o Plano 08.5-07 (onda 4), consumindo o `layout-context` criado aqui.
# Nada a mudar neste plano por causa disso — só não estranhar a ausência.
#
# NÃO usar `Sheet`: decidido no Plano 08.5-02 e no CONTEXT.md. A lista de
# membros ESCONDE, não vira gaveta sobreposta — gaveta é vocabulário de layout
# mobile, e não existe alvo mobile neste projeto.
# NÃO mexer em `minWidth: 900` de `src/main/index.ts`: já provado suficiente
# pela F3, e mexer nisso é mudar o contrato da janela sem dado novo.
#
# Propriedade de arquivo: `AppShell.tsx` foi tocado pelo Plano 08.5-02 (onda
# anterior, montagem do `<Toaster />`) — reler o arquivo antes de editar, não
# assumir o conteúdo que está neste contexto. `ConversationArea.tsx` é do
# Plano 08.5-10, nesta mesma onda: NÃO editar (é por isso que a visibilidade
# dos membros mora num contexto, e não numa prop atravessando a área de
# conversa).
</context>

<tasks>

<task type="auto">
  <name>Task 1: layout-context com visibilidade da lista de membros</name>
  <files>src/renderer/src/state/layout-context.tsx</files>
  <action>
    Criar um contexto pequeno, no mesmo estilo de `selection-context.tsx`
    (provider + hook `useLayout`, erro claro se usado fora do provider):
    ```ts
    type LayoutContextValue = {
      membersVisible: boolean
      toggleMembers: () => void
    }
    ```
    Persistir em `localStorage` sob `janja:layout-preferences` seguindo o mesmo
    padrão defensivo dos outros módulos de preferência do projeto
    (`voice-preferences.ts`, `screenshare-preferences.ts`): try/catch, default
    `membersVisible: true`, dado corrompido volta ao default. Se a persistência
    ficar grande, extrair para `src/renderer/src/lib/layout-preferences.ts` —
    mas NÃO criar um teste novo só para isso: o contrato é idêntico ao já
    testado nos outros dois módulos, e o valor de repetir o teste é baixo.

    Escopo do contexto: SÓ visibilidade da lista de membros. Não transformar
    isso num "contexto de UI" genérico — o próximo item entra quando existir,
    não antes.
  </action>
  <verify>`npm run typecheck` limpo; `grep -n "membersVisible" src/renderer/src/state/layout-context.tsx` retorna o valor e o toggle.</verify>
  <done>Existe um provider de layout com `membersVisible` persistido por máquina.</done>
</task>

<task type="auto">
  <name>Task 2: AppShell monta o provider e condiciona a coluna de membros</name>
  <files>src/renderer/src/components/shell/AppShell.tsx</files>
  <action>
    Reler o arquivo (o Plano 08.5-02 acrescentou o `<Toaster />`) e:
    1. Envolver `ShellBody` com o `LayoutProvider`, ao lado dos providers já
       existentes (`SelectionProvider`, `VoiceProvider`, `TooltipProvider`).
    2. Em `ShellBody`, na visão `view === 'server'`, renderizar a coluna
       `<div className="flex-none w-60 ...."><MemberList /></div>` apenas
       quando `membersVisible`. A coluna central já tem `flex-1 min-w-0`, então
       ela absorve o espaço automaticamente — não acrescentar largura fixa nem
       cálculo de porcentagem.
    3. NÃO renderizar a coluna com `hidden`/`w-0`: desmontar de verdade. A
       `MemberList` mantém duas subscriptions do Convex (`listServerMembers` e
       `voiceParticipantsByServer`); esconder por CSS manteria as duas vivas
       sem ninguém olhando.
    4. Não mexer no branch `view === 'home'` (a Home nunca teve lista de
       membros — decisão registrada em `06-RESEARCH.md §7`).
    5. Não editar mais nada neste arquivo.
  </action>
  <verify>`grep -n "membersVisible" src/renderer/src/components/shell/AppShell.tsx` retorna a condição; `grep -c "Toaster" ...` continua 1 (o trabalho do Plano 02 sobreviveu); `npm run build` passa.</verify>
  <done>A coluna de membros aparece e desaparece pelo estado do contexto, desmontando de verdade.</done>
</task>

<task type="auto">
  <name>Task 3: Cabeçalho do canal aguenta janela estreita, e rail marca o ativo com destaque</name>
  <files>src/renderer/src/components/shell/ChannelHeader.tsx, src/renderer/src/components/shell/ServerRail.tsx</files>
  <action>
    **ChannelHeader.tsx:**
    1. `truncate` + `min-w-0` no `<span>` do nome do canal e no container flex
       (hoje a linha 37 não tem truncate — o RESEARCH aponta isso como o
       primeiro a estourar quando botões entram).
    2. Acrescentar, à direita, um botão de alternar a lista de membros: ícone
       `Users` do lucide, `Button variant="ghost" size="icon-sm"`, dentro de
       `Tooltip` (convenção do brief: ícone sem texto visível ganha tooltip),
       com `aria-label` ("Mostrar lista de membros" / "Esconder lista de
       membros", alternando conforme o estado) e `aria-pressed={membersVisible}`.
       Mesmo padrão dos botões que já existem no cabeçalho da sidebar.
    3. **NÃO adicionar** os botões de notificações, pins e busca que o brief
       original lista: não existe backend para nenhum dos três (busca no
       histórico é CHAT-13, explicitamente fora do v1). Botão que não faz nada
       é pior que botão ausente, e ocupa exatamente o espaço que a janela de
       900px não tem. Registrar essa decisão em comentário no arquivo.
    4. O canal não tem campo de descrição no schema — não inventar um lugar
       para ele. Registrar no SUMMARY (o brief pede "nome e descrição do
       canal"; a descrição não existe como dado).
    5. O botão só faz sentido na visão de servidor. Como `ChannelHeader` só é
       renderizado por `ConversationArea` (visão de servidor), não é preciso
       condicionar — conferir e registrar.

    **ServerRail.tsx:**
    6. Trocar as concatenações de string por `cn()`
       (linhas ~37-39, ~47-50, ~75-78, ~86-89 — `'classe fixa ' + (cond ? 'a' : 'b')`).
    7. Indicador de servidor/Início ativo: a barra vertical e o anel usam
       `bg-foreground` / `ring-foreground` (branco). Trocar por
       `bg-highlight` / `ring-highlight` — "estado ativo/selecionado" é um dos
       três usos autorizados do tom de destaque, e é o mesmo vocabulário que o
       Plano 08.5-08 aplica no canal selecionado.
    8. Não mexer em mais nada do rail: os itens já são `<button>` de verdade,
       com `aria-label` e `aria-current`, e os tooltips já existem.
  </action>
  <verify>
    `grep -n "truncate" src/renderer/src/components/shell/ChannelHeader.tsx` retorna o nome do canal;
    `grep -n "aria-pressed" src/renderer/src/components/shell/ChannelHeader.tsx` retorna o botão;
    `grep -cE "' \+ \(" src/renderer/src/components/shell/ServerRail.tsx` retorna 0 (nenhuma concatenação condicional sobrou);
    `grep -n "highlight" src/renderer/src/components/shell/ServerRail.tsx` retorna barra e anel;
    `npm run build` passa.
  </verify>
  <done>O cabeçalho trunca o nome, tem um botão real de alternar membros e nenhum botão morto; o rail usa `cn()` e marca o ativo com o destaque.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` limpo; `npm run build` passa; `npx vitest run` verde.
- `grep -rn "Sheet" src/renderer/src/components/` vazio — a decisão de não
  introduzir gaveta continua valendo.
- `git diff --stat src/main/index.ts` vazio — `minWidth: 900` intocado.

**O que NÃO é verificável neste ambiente (WSL2, sem janela):**
- Que o layout aguenta 900×600 de verdade, com e sem a lista de membros.
  Esta é a verificação mais barata que existe e não precisa de matemática —
  só redimensionar a janela até o mínimo — e é item obrigatório do roteiro do
  Plano 08.5-15.
- Que o nome de canal longo trunca em vez de empurrar o botão.
- Que a coluna de membros reaparece no lugar certo (nada de layout shift).
</verification>

<success_criteria>
A moldura sobrevive à janela estreita: membros alternáveis com persistência,
nome de canal truncado, nenhum botão decorativo novo, e o servidor ativo
marcado com o tom de destaque.
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/08.5-repaginacao-da-ui/08.5-05-SUMMARY.md`,
registrando a decisão sobre os botões não implementados (notificações, pins,
busca) e a ausência de descrição de canal no schema.
</output>
