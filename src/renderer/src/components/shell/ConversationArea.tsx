// Stub mínimo (Plano 03-01). O Plano 03-03 substitui isto pela área de
// conversa real (cabeçalho de canal, lista de mensagens, campo de texto).
export function ConversationArea(): React.JSX.Element {
  return (
    <div className="h-full flex items-center justify-center text-sm text-muted-foreground">
      Área de conversa
    </div>
  )
}
